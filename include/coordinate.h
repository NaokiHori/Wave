#if !defined(COORDINATE_H)
#define COORDINATE_H

#include <stddef.h> // size_t

extern double get_coordinate (
    const double length,
    const size_t npoints,
    const size_t index
);

#endif // COORDINATE_H
